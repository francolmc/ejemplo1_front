import MainTemplate from "../templates/MainTemplate";

const About = () => {
    return (
        <MainTemplate>
            Hola, esta es la pagina acerca de... o sobre nosotros.
        </MainTemplate>
    );
};

export default About;